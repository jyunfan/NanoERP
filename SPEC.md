# NanoERP 規格整理

本規格以重現舊 DOS 版操作行為為優先，再映射到目前 Python Textual 實作。若本檔與舊 UI 截圖衝突，以 `doc/UI/` 的畫面規格為準。

## 專案目標

NanoERP 是以鍵盤快速輸入為主的傳統 ERP/TUI 系統，第一階段目標是重現舊 DOS 版核心流程：

- 客戶資料設定
- 產品資料設定
- 客戶訂單
- 廠商進貨/退貨
- 總數核對與基礎報表
- 過帳與日報表
- 結帳、期報表與歷史查帳

## 技術基礎

| 項目 | 內容 |
| --- | --- |
| UI framework | Python Textual |
| 資料庫 | SQLite，現有 schema 參考 `db.sql` 與 `doc/DB.md` |
| 進入點 | `main.py` |
| 樣式 | `nanoerp.tcss` |
| 主要設計原則 | 鍵盤優先、DOS 風格資料表、固定底部狀態列 |

## 全域狀態

| 狀態 | 說明 |
| --- | --- |
| `WORK_DATE` / `app.work_date` | 作業日期。預設今天，可由全域快捷鍵或畫面功能鍵更改。 |
| 市場/類別 | 主要市場為 `1=其餘市場`、`2=建國市場`、`3=南部市場`；部分畫面另有 `4=廠商`。 |

日期顯示需支援舊系統格式，例如 `115年 4月 7日 星期二`。內部可使用 ISO 日期。

## 全域 UI 原則

具體畫面配置、欄位與按鍵以 `doc/UI/` 為準。本檔只保留跨畫面共同原則：

- 目前列以高亮底色顯示。
- 右側功能鍵或代碼說明面板固定可見。
- 底部固定顯示作業日期、市場切換與功能鍵提示。
- 數字鍵可直接選擇底部選項。

## 選單結構

```text
主選單
├── 1 客戶資料設定
│   ├── 0 回上一頁
│   ├── 1 其餘市場
│   ├── 2 建國市場
│   ├── 3 南部市場
│   └── 4 廠商
├── 2 產品資料設定
├── 3 進銷訂貨處理
│   ├── 0 回主系統
│   ├── 1 客戶訂單
│   └── 2 廠商訂單
├── 4 過帳與日報表
│   ├── 1 過帳
│   └── 2 日報表
├── 5 結帳與期報表
│   ├── 1 查帳
│   │   ├── 1 進銷明細
│   │   └── 2 營業總額
│   └── 2 列印
├── 6 抄貨報表製作
└── 7 系統維護檢查
```

目前 `doc/MENU.md` 與舊截圖在部分分支上不同；選單與畫面行為以 `doc/UI/` 整理結果為準。

## 全域快捷鍵

| 按鍵 | 行為 |
| --- | --- |
| 方向鍵 | 在目前清單或表格中移動。 |
| 數字鍵 | 選擇目前畫面底部選項或選單項目。 |
| `Enter` | 選定、進入編輯或確認輸入。 |
| `Tab` | 在左右區塊間切換，特別是訂單畫面。 |
| `Esc` | 回跳、取消編輯或離開目前畫面。 |
| `F10` | 更改作業日期，目前 Textual 版可保留此全域鍵。 |
| `Q` | 離開系統，目前 Textual 版可保留此全域鍵。 |

## UI 規格文件

舊畫面已拆成以下 Markdown 規格：

| 文件 | 內容 |
| --- | --- |
| `doc/UI/UI-1-客戶資料設定.md` | 客戶資料設定畫面、結帳代碼、功能鍵。 |
| `doc/UI/UI-2-產品資料設定.md` | 產品資料表、價格、安存、銷退單位與列印功能。 |
| `doc/UI/UI-3-進銷訂貨處理.md` | 客戶訂單、產品總數、總數核對、廠商進退貨。 |
| `doc/UI/UI-4-過帳與日報表.md` | 過帳、客戶退貨、日報表、日帳單列印設定。 |
| `doc/UI/UI-5-結帳與期報表.md` | 查帳、進銷明細、營業總額、結帳週期列印。 |

## 功能模組摘要

本節只描述模組責任與跨畫面資料規則。畫面配置、欄位、按鍵與範例文字以 `doc/UI/` 為準，不在本檔重複維護。

### 1. 客戶資料設定

- 維護客戶主檔、所屬市場、結帳代碼與聯絡資料。
- 客戶畫面可切換市場與廠商資料；詳細操作見 `doc/UI/UI-1-客戶資料設定.md`。

結帳代碼為跨報表共用資料：

| 代碼 | 名稱 | 用途 |
| --- | --- | --- |
| 0 | 不印 | 不產生報表。 |
| 1 | 出貨 | 出貨單或當日出貨類報表。 |
| 2 | 日 | 日結報表。 |
| 3 | 週 | 週結報表，日期規則待確認。 |
| 4 | 旬 | 旬結報表，日期規則待確認。 |
| 5 | 半月 | 半月結報表。 |
| 6 | 月 | 月結報表。 |

### 2. 產品資料設定

- 維護產品主檔、簡稱、進價、售價、安全庫存與銷退單位。
- `product.short_name` 是訂單、報表與查帳畫面的主要顯示名稱。
- 詳細操作見 `doc/UI/UI-2-產品資料設定.md`。

### 3. 進銷訂貨處理

- 客戶訂單寫入過帳前暫存資料。
- 廠商進退貨寫入進貨/退貨資料，並以客戶訂單彙總銷貨量。
- 客戶訂單的產品總數與總數核對用於過帳前檢查。
- 詳細操作見 `doc/UI/UI-3-進銷訂貨處理.md`。

客戶訂單資料邏輯：

| 資料 | 說明 |
| --- | --- |
| `customer_freq_product` | 決定客戶訂單畫面出現哪些產品。 |
| `order_draft` | 記錄過帳前暫存的客戶訂單。 |
| `order_table` | 記錄過帳後正式客戶訂單，含工作日期與價格快照。 |
| `customer_product` | 記錄客戶專用售價。 |

數量規則：

- 數量輸入必須是數字。
- 數量為 `0` 時不計入交易數與產品總數。
- 第一階段可採「數量 0 刪除訂單記錄」或「保留 0 記錄但查詢排除」，但需在實作中固定一致。

廠商進退貨資料邏輯：

| 資料 | 說明 |
| --- | --- |
| `supplier` | 廠商主檔。 |
| `supplier_freq_product` | 決定廠商進退貨畫面出現哪些產品。 |
| `purchase_order` | 記錄廠商進貨/退貨。 |
| `order_table` | 用於計算各市場銷貨量。 |
| `product.safety_stock` | 安全庫存。 |
| `product.return_unit` | 退銷比/銷退單位。 |

### 4. 過帳與日報表

- 過帳將過帳前暫存訂單轉成正式明細，建立可追溯批次。
- 日報表以過帳後正式資料為準。
- 詳細操作見 `doc/UI/UI-4-過帳與日報表.md`。

過帳資料邏輯：

| 資料 | 說明 |
| --- | --- |
| `order_draft` | 過帳來源資料，含客戶訂單與可能的退貨資料。 |
| `posting_batch` | 過帳批次資料，記錄工作日期與過帳時間。 |
| `order_table` | 過帳後正式訂單明細。 |

報表金額規則：

- 明細金額優先使用 `order_table.sale_price` 過帳價格快照。
- 舊資料缺少價格快照時，才 fallback 到客戶專用售價或產品售價。

### 5. 結帳與期報表

- 第 5 章包含歷史查帳與結帳週期報表列印，不是單一結帳動作。
- 查帳包含進銷明細與營業總額。
- 進銷明細以三個維度查詢：客戶、歷史日期交易額、品項明細。
- 詳細操作見 `doc/UI/UI-5-結帳與期報表.md`。

結帳/期報表資料邏輯：

- 進銷明細、營業總額與期報表都以過帳後正式資料 `order_table` 為準。
- 日期交易額 = 同一客戶、同一交易日期的正式明細金額加總。
- 區間營業總額 = 所選範圍內日期交易額加總。
- 查帳可瀏覽歷史交易日期，不限於目前作業日期。
- 列印區間以作業日期推算，並依客戶 `checkout_code` 決定實際起迄。

價格規則：

- 若 `customer_product` 有客戶專用售價，優先使用。
- 否則使用 `product.sale_price`。
- 客戶訂單售價模式只允許修改 `customer_product.sale_price`。
- `product.purchase_price` 在售價模式只作為參考顯示，不可修改。

結帳區間規則：

| 代碼 | 方式 | 區間規則 |
| --- | --- | --- |
| 0 | 不印 | 不產生報表。 |
| 1 | 出貨 | 作業日期當天。 |
| 2 | 日 | 作業日期當天。 |
| 3 | 週 | 待確認。 |
| 4 | 旬 | 待確認。 |
| 5 | 半月 | 1-15 日或 16 日至月底。 |
| 6 | 月 | 當月 1 日至月底。 |

## 資料庫摘要

| 資料表 | 用途 |
| --- | --- |
| `customer` | 客戶主檔、市場、結帳代碼。 |
| `supplier` | 廠商主檔。 |
| `product` | 產品主檔、價格、庫存基礎資料。 |
| `order_draft` | 過帳前客戶訂單暫存。 |
| `posting_batch` | 過帳批次資料。 |
| `order_table` | 過帳後正式客戶訂單。 |
| `customer_product` | 客戶專用售價。 |
| `customer_freq_product` | 客戶常用/訂單產品清單。 |
| `supplier_freq_product` | 廠商常用/進退貨產品清單。 |
| `purchase_order` | 廠商進貨與退貨。 |

## 第一階段實作原則

1. 先讓畫面結構與鍵盤操作貼近舊 DOS 版。
2. 客戶、產品、訂單、進退貨的資料流要完整可用。
3. 列印、移位、檢查、進貨建議可先做成明確 placeholder，但畫面功能鍵需保留。
4. 所有數量與價格欄位必須做數字輸入保護。
5. 不確定的舊系統規則需在文件中標示，不直接用現代化假設覆蓋。

## 待確認議題

- 客戶與產品畫面的「代碼」是否直接等於 SQLite 主鍵，或需保留舊系統獨立代碼欄位。
- 訂單數量為 0 時，要刪除訂單記錄或保留 0 記錄。
- 週結與旬結的精確日期區間。
- 廠商進退貨中的「昨存」「今存」「進貨建議」公式。
- `銷退單位` / `退銷比` 的格式與計算規則。
- 客戶退貨的 `退價` 欄位格式與資料表位置。
