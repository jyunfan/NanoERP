# NanoERP 規格整理

本規格以重現舊 DOS 版操作行為為優先，再映射到目前 Python Textual 實作。若本檔與舊 UI 截圖衝突，以 `doc/UI/` 的畫面規格為準。

## 專案目標

NanoERP 是以鍵盤快速輸入為主的傳統 ERP/TUI 系統，第一階段目標是重現舊 DOS 版核心流程：

- 客戶資料設定
- 產品資料設定
- 客戶訂單
- 廠商進貨/退貨
- 總數核對與基礎報表
- 過帳與日報表

## 技術基礎

| 項目 | 內容 |
| --- | --- |
| UI framework | Python Textual |
| 資料庫 | SQLite，現有 schema 參考 `db.sql` 與 `doc/DB.md` |
| 進入點 | `main.py` |
| 樣式 | `nanoerp.tcss` |
| 主要設計原則 | 鍵盤優先、DOS 風格資料表、固定底部狀態列 |

## 全域狀態

| 狀態 | 說明 |
| --- | --- |
| `WORK_DATE` / `app.work_date` | 作業日期。預設今天，可由全域快捷鍵或畫面功能鍵更改。 |
| 市場 | `1=其餘市場`、`2=建國市場`、`3=南部市場`。 |

日期顯示需支援舊系統格式，例如 `115年 4月 7日 星期二`。內部可使用 ISO 日期。

## 全域 UI 版型

舊 DOS 版主要畫面共有固定結構：

```text
┌──────────────────────────────────────────────────────────────┐
│ 主資料區                                                     │
│                                                              │
│ 依畫面顯示資料表、左右分割清單、右側代碼/功能面板             │
│                                                              │
└──────────────────────────────────────────────────────────────┘
作業日期 115年 4月 7日 星期二
0.回主系統 / 回上系統  1.其餘市場  2.建國市場  3.南部市場  ...
功能鍵提示列
輸入法/系統狀態列
```

重現時應優先保留：

- 目前列以高亮底色顯示。
- 右側功能鍵或代碼說明面板固定可見。
- 底部固定顯示作業日期、市場切換與功能鍵提示。
- 數字鍵可直接選擇底部選項。

## 選單結構

```text
主選單
├── 1 客戶資料設定
│   ├── 0 回上一頁
│   ├── 1 其餘市場
│   ├── 2 建國市場
│   ├── 3 南部市場
│   └── 4 廠商
├── 2 產品資料設定
├── 3 進銷訂貨處理
│   ├── 0 回主系統
│   ├── 1 客戶訂單
│   └── 2 廠商訂單
├── 4 過帳與日報表
│   ├── 1 過帳
│   └── 2 日報表
├── 5 結帳與其報表
│   └── 1 結帳
├── 6 抄貨報表製作
└── 7 系統維護檢查
```

目前 `doc/MENU.md` 與舊截圖在部分分支上不同，第一階段以截圖可確認的 `1/2/3` 流程為準。

## 全域快捷鍵

| 按鍵 | 行為 |
| --- | --- |
| 方向鍵 | 在目前清單或表格中移動。 |
| 數字鍵 | 選擇目前畫面底部選項或選單項目。 |
| `Enter` | 選定、進入編輯或確認輸入。 |
| `Tab` | 在左右區塊間切換，特別是訂單畫面。 |
| `Esc` | 回跳、取消編輯或離開目前畫面。 |
| `F10` | 更改作業日期，目前 Textual 版可保留此全域鍵。 |
| `Q` | 離開系統，目前 Textual 版可保留此全域鍵。 |

## UI 規格文件

舊畫面已拆成以下 Markdown 規格：

| 文件 | 內容 |
| --- | --- |
| `doc/UI/UI-1-客戶資料設定.md` | 客戶資料設定畫面、結帳代碼、功能鍵。 |
| `doc/UI/UI-2-產品資料設定.md` | 產品資料表、價格、安存、銷退單位與列印功能。 |
| `doc/UI/UI-3-進銷訂貨處理.md` | 客戶訂單、產品總數、總數核對、廠商進退貨。 |
| `doc/UI/UI-4-過帳與日報表.md` | 過帳、客戶退貨、日報表、日帳單列印設定。 |

## 1. 客戶資料設定

詳細畫面規格見 `doc/UI/UI-1-客戶資料設定.md`。

### 資料欄位

| 欄位 | DB 欄位 | 說明 |
| --- | --- | --- |
| 代碼 | `customer.id` | 客戶代碼。 |
| 車 | `customer.car_number` | 車次或排序用數字。 |
| 名稱 | `customer.name` | 客戶名稱。 |
| 結帳碼 | `customer.checkout_code` | 結帳代碼。 |
| 電話(1) | `customer.phone1` | 第一電話。 |
| 電話(2) | `customer.phone2` | 第二電話。 |
| 市場 | `customer.market` | 市場代碼。 |

### 結帳代碼

| 代碼 | 名稱 |
| --- | --- |
| 0 | 不印 |
| 1 | 出貨 |
| 2 | 日 |
| 3 | 週 |
| 4 | 旬 |
| 5 | 半月 |
| 6 | 月 |

### 功能

| 按鍵 | 行為 |
| --- | --- |
| `Alt+F1` | 增加。 |
| `Alt+F3` | 移位。 |
| `Alt+F5` | 明細列印。 |
| `Alt+F6` | 電話列印。 |
| `Alt+F9` | 刪除。 |
| `0` | 更改。 |
| `Esc` | 回跳。 |

## 2. 產品資料設定

詳細畫面規格見 `doc/UI/UI-2-產品資料設定.md`。

### 資料欄位

| 欄位 | DB 欄位 | 說明 |
| --- | --- | --- |
| 代碼 | `product.id` | 產品代碼。 |
| 車 | `product.car_number` | 車次或排序用數字。 |
| 詳細名稱 | `product.detailed_name` | 完整產品名稱。 |
| 簡稱 | `product.short_name` | 訂單畫面使用。 |
| 進價 | `product.purchase_price` | 進貨單價。 |
| 售價 | `product.sale_price` | 銷售單價。 |
| 安存 | `product.safety_stock` | 安全庫存。 |
| 銷退單位 | `product.return_unit` | 銷退比例或單位字串。 |

### 功能

| 按鍵 | 行為 |
| --- | --- |
| `F1` | 增加產品。 |
| `F3` | 移位。 |
| `F5` | 印詳細表。 |
| `F6` | 印報價單。 |
| `F9` | 刪除。 |
| `Esc` | 回主系統。 |

## 3. 進銷訂貨處理

詳細畫面規格見 `doc/UI/UI-3-進銷訂貨處理.md`。

### 3.0 入口

| 選項 | 行為 |
| --- | --- |
| `0` | 回主系統。 |
| `1` | 客戶訂單。 |
| `2` | 廠商訂單。 |

### 3.1 客戶訂單

核心行為：

- 左側顯示客戶與交易數。
- 中央顯示選定客戶的產品與數量。
- 可切換顯示產品總數。
- 底部可切換市場與進入總數核對。
- 可維護客戶產品清單、數量與客戶售價。

資料邏輯：

| 資料 | 說明 |
| --- | --- |
| `customer_freq_product` | 決定客戶訂單畫面出現哪些產品。 |
| `order_draft` | 記錄過帳前暫存的客戶訂單。 |
| `order_table` | 記錄過帳後正式客戶訂單，含工作日期與價格快照。 |
| `customer_product` | 記錄客戶專用售價。 |

數量規則：

- 數量輸入必須是數字。
- 數量為 `0` 時不計入交易數與產品總數。
- 第一階段可採「數量 0 刪除訂單記錄」或「保留 0 記錄但查詢排除」，但需在實作中固定一致。

### 3.1.4 總數核對

用途是跨市場檢查每個產品的訂單總量。

| 欄位 | 說明 |
| --- | --- |
| 其餘市場 | 其餘市場該產品數量。 |
| 建國市場 | 建國市場該產品數量。 |
| 南部市場 | 南部市場該產品數量。 |
| 品名 | 產品簡稱。 |
| 總數量 | 三市場合計。 |

### 3.2 廠商進退貨

核心行為：

- 左側顯示廠商與交易數。
- 中央顯示產品、銷貨、進貨、退貨。
- 右側顯示選定產品在各市場的銷貨量、安存、昨存、今存、退銷比與退貨。
- 支援進貨、退貨、昨存修改與進貨建議。

資料邏輯：

| 資料 | 說明 |
| --- | --- |
| `supplier` | 廠商主檔。 |
| `supplier_freq_product` | 決定廠商進退貨畫面出現哪些產品。 |
| `purchase_order` | 記錄廠商進貨/退貨。 |
| `order_table` | 用於計算各市場銷貨量。 |
| `product.safety_stock` | 安全庫存。 |
| `product.return_unit` | 退銷比/銷退單位。 |

## 4. 過帳與日報表

詳細畫面規格見 `doc/UI/UI-4-過帳與日報表.md`。

### 4.0 入口

| 選項 | 行為 |
| --- | --- |
| `0` | 回主系統。 |
| `1` | 過帳。 |
| `2` | 日報表。 |

### 4.1 過帳

過帳流程包含兩個入口：

| 選項 | 行為 |
| --- | --- |
| `1` | 客戶退貨/過帳前退貨資料確認。 |
| `2` | 執行過帳。 |

客戶退貨畫面：

- 左側選客戶。
- 中間選品名。
- 右側顯示退貨明細，欄位包含客戶、品名、退價、退量、金額。
- 底部可切換市場。

執行過帳畫面：

- 顯示過帳對象與完成率。
- 按 `Space` 才執行過帳。
- `Esc` 取消。

資料邏輯：

| 資料 | 說明 |
| --- | --- |
| `order_draft` | 過帳來源資料，含客戶訂單與可能的退貨資料。 |
| `posting_batch` | 過帳批次資料，記錄工作日期與過帳時間。 |
| `order_table` | 過帳後正式訂單明細。 |

### 4.2 日報表

日報表流程包含：

| 選項 | 行為 |
| --- | --- |
| `1` | 出貨單。 |
| `2` | 日帳單。 |

### 4.2.1 日帳單

日帳單包含：

| 選項 | 行為 |
| --- | --- |
| `1` | 列印者設定。 |
| `2` | 執行。 |

列印者設定核心行為：

- 依市場列出客戶、交易數與列印狀態。
- `●` 表示印，`○` 表示不印。
- `0` 切換目前客戶印/不印。
- `Alt+F1` 至 `Alt+F6` 依結帳代碼批次選定。
- `Ctrl+Home` 本市場全部不印。
- `Ctrl+End` 本市場恢復內定。

內定值：

| 報表 | 預設結帳代碼 |
| --- | --- |
| 出貨單 | F1-F6，對應出貨、日、週、旬、半月、月。 |
| 日帳單 | 僅 F2，即日結。 |

需要確認：

- 客戶退貨的 `退價` 欄位格式與資料表位置。

## 5. 結帳與其報表

結帳依客戶 `checkout_code` 決定區間。

| 代碼 | 方式 | 區間規則 |
| --- | --- | --- |
| 0 | 不印 | 不產生報表。 |
| 1 | 出貨 | 作業日期當天。 |
| 2 | 日 | 作業日期當天。 |
| 3 | 週 | 待確認。 |
| 4 | 旬 | 待確認。 |
| 5 | 半月 | 1-15 日或 16 日至月底。 |
| 6 | 月 | 當月 1 日至月底。 |

價格規則：

- 若 `customer_product` 有客戶專用售價，優先使用。
- 否則使用 `product.sale_price`。

## 資料庫摘要

| 資料表 | 用途 |
| --- | --- |
| `customer` | 客戶主檔、市場、結帳代碼。 |
| `supplier` | 廠商主檔。 |
| `product` | 產品主檔、價格、庫存基礎資料。 |
| `order_draft` | 過帳前客戶訂單暫存。 |
| `posting_batch` | 過帳批次資料。 |
| `order_table` | 過帳後正式客戶訂單。 |
| `customer_product` | 客戶專用售價。 |
| `customer_freq_product` | 客戶常用/訂單產品清單。 |
| `supplier_freq_product` | 廠商常用/進退貨產品清單。 |
| `purchase_order` | 廠商進貨與退貨。 |

## 第一階段實作原則

1. 先讓畫面結構與鍵盤操作貼近舊 DOS 版。
2. 客戶、產品、訂單、進退貨的資料流要完整可用。
3. 列印、移位、檢查、進貨建議可先做成明確 placeholder，但畫面功能鍵需保留。
4. 所有數量與價格欄位必須做數字輸入保護。
5. 不確定的舊系統規則需在文件中標示，不直接用現代化假設覆蓋。

## 待確認議題

- 客戶與產品畫面的「代碼」是否直接等於 SQLite 主鍵，或需保留舊系統獨立代碼欄位。
- 訂單數量為 0 時，要刪除訂單記錄或保留 0 記錄。
- 日報表應顯示過帳前或過帳後資料。
- 週結與旬結的精確日期區間。
- 廠商進退貨中的「昨存」「今存」「進貨建議」公式。
- `銷退單位` / `退銷比` 的格式與計算規則。
